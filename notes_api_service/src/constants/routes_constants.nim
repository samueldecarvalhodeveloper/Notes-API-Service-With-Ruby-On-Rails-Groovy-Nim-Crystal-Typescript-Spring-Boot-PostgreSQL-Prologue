const INDEX_ROUTE* = "/"

const USER_ROUTE* = "/users"

const USER_ROUTE_BY_ID* = "/{id}/"

const NOTE_ROUTE* = "/notes"

const NOTE_ROUTE_BY_USER_ID* = "/{userId}/"

const NOTE_ROUTE_BY_NOTE_ID* = "/{userId}/{id}/"
